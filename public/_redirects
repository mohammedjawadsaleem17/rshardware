# Redirect all routes to index.html for client-side routing
/*    /index.html   200

# Block netlify subdomain (add this at the end)
https://rshardware.netlify.app/*  https://rshardwares.in/:splat  301!